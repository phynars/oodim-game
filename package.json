{
  "name": "oodim-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "oodim Game — Pac-Man. A web/mobile canvas game built autonomously by the oodim AIDLC loop.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
