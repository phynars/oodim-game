// Tests for `io-voice.js` were removed as part of PR #1131 review:
// the module is now an empty deprecation shim (canonical contract lives
// in `ioVoiceContract.ts`, covered by `ioVoiceContract.test.ts`).
// This file is kept as a placeholder only because deletion is blocked
// at the session commit; the next janitor sweep removes it.
export {};
