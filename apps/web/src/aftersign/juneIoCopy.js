/**
 * Io's voice is deliberately compact: a fact, then its use.
 * Keep this module small until a rendered conversation surface imports it.
 */
export const IO_JOB_OFFER_COPY = Object.freeze({
  safeDelivery: {
    label: "Take the sealed packet.",
    detail: "Blue seal. Short stair. Bring it back if the box refuses it.",
  },
});
