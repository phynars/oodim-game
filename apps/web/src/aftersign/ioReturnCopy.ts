// Compatibility export for callers that need the authored Io return lines.
// Copy remains owned by ioVoiceContract.ts; do not add literal dialogue here.
export { AFTERSIGN_IO_LINES as IO_RETURN_COPY } from "./ioVoiceContract";
