# Automated dependency-update PRs for the public oodim-game repo (added
# 2026-07-09 hardening). Security updates + grouped weekly version bumps so
# vulnerable deps surface as PRs instead of going stale. Grouped to avoid the
# per-dep PR spam that hit the sibling repos (2026-07-04).
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    # (#959 resolved 2026-08-08: the wrangler >4.113 miniflare-persistence
    # break was fixed upstream — full agar polarity matrix verified green on
    # 4.120.0. The redgreen workflow gates future bumps; no ignores needed.)
    groups:
      dev-dependencies:
        dependency-type: "development"
      production-dependencies:
        dependency-type: "production"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 3
